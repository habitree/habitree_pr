# Readtree 독서플랫폼 기능명세서 (Functional Specification)

## 📋 문서 정보

| 항목 | 내용 |
|------|------|
| **프로젝트명** | Readtree 독서플랫폼 (Readtree by Habitree) |
| **버전** | 3.0.0 (Supabase + Vercel 통합 버전) |
| **작성일** | 2025년 12월 |
| **작성자** | 개발팀 |
| **참고 문서** | PRD- Readtree 독서플랫폼.md, user_stories.md, 구버전-기능명세서.md |

---

## 1. 개요

### 1.1 프로젝트 목적
Readtree는 개인의 독서 여정을 체계적으로 기록하고 관리하는 웹 애플리케이션입니다. 사용자가 읽은 책을 기록하고, 인상 깊은 문장을 필사·사진·메모로 남기며, 자동으로 정리되고 검색 가능한 독서 플랫폼을 제공합니다.

### 1.2 주요 목표
- 독서 기록의 체계적 관리 및 자동 정리
- 필사·사진·메모를 통한 인상적인 구절 보존
- 문장 단위 검색을 통한 기록 재발견
- 독서 타임라인 시각화
- 독서 커뮤니티 형성 지원 (독서모임)
- SNS 공유 기능

### 1.3 기술 스택

#### Frontend & Backend
- **Framework**: Next.js 15+ (App Router)
- **Language**: TypeScript
- **Styling**: Tailwind CSS
- **State Management**: React Context API 또는 Zustand
- **API Routes**: Next.js API Routes (서버 사이드 로직)

#### Database & Storage
- **Authentication**: Supabase Authentication (카카오/구글 소셜 로그인 지원)
- **Database**: Supabase PostgreSQL (관계형 데이터베이스)
- **Storage**: Supabase Storage (이미지 저장)
- **Real-time**: Supabase Realtime (실시간 업데이트)

#### External Services
- **이미지 처리**: OCR (Optical Character Recognition) API
- **책 정보**: 책 정보 API (예: 알라딘, 교보문고, 네이버 도서)
- **AI/LLM**: Google Gemini API (고급 검색, 의미 분석 등)
- **인프라**: Vercel (서버리스 배포 및 호스팅)

#### 배포 및 인프라
- **호스팅**: Vercel
- **배포 방식**: Git 연동 자동 배포
- **환경 변수 관리**: Vercel 환경 변수 설정
- **도메인**: Vercel 제공 도메인 또는 커스텀 도메인

---

## 2. 사용자 인증 및 계정 관리

### 2.1 로그인 기능

#### 2.1.1 카카오 OAuth 2.0 로그인
**기능 설명**: 카카오 계정을 이용한 소셜 로그인 기능

**기술 구현**:
- Supabase Auth의 OAuth Provider 사용
- 카카오 개발자 콘솔에서 OAuth 앱 등록
- Redirect URL 설정: `https://{project-url}/auth/callback`

**입력 항목**:
- 카카오 계정 정보 (카카오에서 제공)

**처리 과정**:
1. 사용자가 "카카오 로그인" 버튼 클릭
2. Supabase Auth의 `signInWithOAuth()` 호출
3. 카카오 인증 페이지로 리다이렉트
4. 사용자 인증 후 콜백 URL로 리다이렉트
5. Supabase가 자동으로 사용자 정보 저장
6. `users` 테이블에 프로필 정보 저장 (트리거 또는 API Route)
7. 메인 페이지로 리다이렉트

**출력 항목**:
- 로그인 상태 업데이트
- 사용자 프로필 정보 표시

**에러 처리**:
- 인증 실패 시 에러 메시지 표시
- 네트워크 오류 시 재시도 안내

**권한**: 공개 (모든 사용자)

---

#### 2.1.2 구글 OAuth 2.0 로그인
**기능 설명**: 구글 계정을 이용한 소셜 로그인 기능

**기술 구현**:
- Supabase Auth의 Google OAuth Provider 사용
- Google Cloud Console에서 OAuth 클라이언트 ID 생성
- Redirect URL 설정: `https://{project-url}/auth/callback`

**입력 항목**:
- 구글 계정 정보 (구글에서 제공)

**처리 과정**:
1. 사용자가 "구글 로그인" 버튼 클릭
2. Supabase Auth의 `signInWithOAuth({ provider: 'google' })` 호출
3. 구글 인증 페이지로 리다이렉트
4. 사용자 인증 후 콜백 URL로 리다이렉트
5. Supabase가 자동으로 사용자 정보 저장
6. `users` 테이블에 프로필 정보 저장
7. 메인 페이지로 리다이렉트

**출력 항목**:
- 로그인 상태 업데이트
- 사용자 프로필 정보 표시

**에러 처리**:
- 인증 실패 시 에러 메시지 표시
- 네트워크 오류 시 재시도 안내

**권한**: 공개 (모든 사용자)

---

#### 2.1.3 이메일/비밀번호 로그인
**기능 설명**: 이메일과 비밀번호를 이용한 로그인 기능

**입력 항목**:
- 이메일 주소 (필수, 이메일 형식 검증)
- 비밀번호 (필수, 최소 6자)

**처리 과정**:
1. 사용자가 이메일과 비밀번호 입력
2. Supabase Auth의 `signInWithPassword()` 호출
3. 인증 성공 시 사용자 정보 로드
4. 로그인 상태 업데이트

**출력 항목**:
- 로그인 성공 시 메인 페이지로 이동
- 사용자 프로필 정보 표시

**에러 처리**:
- 잘못된 이메일/비밀번호: "이메일 또는 비밀번호가 올바르지 않습니다"
- 이메일 미인증: "이메일 인증이 필요합니다"
- 네트워크 오류: "네트워크 오류가 발생했습니다"

**권한**: 공개 (모든 사용자)

---

### 2.2 회원가입 기능

**기능 설명**: 새로운 사용자 계정 생성

**입력 항목**:
- 이메일 주소 (필수, 이메일 형식 검증, 중복 확인)
- 비밀번호 (필수, 최소 6자)
- 비밀번호 확인 (필수, 비밀번호와 일치 확인)
- 닉네임 (선택)
- 약관 동의 (필수)

**처리 과정**:
1. 입력 정보 유효성 검증
2. 이메일 중복 확인 (Supabase Auth)
3. Supabase Auth의 `signUp()` 호출
4. 이메일 인증 메일 발송 (Supabase 자동 처리)
5. `users` 테이블에 프로필 정보 저장 (Database Trigger 또는 API Route)
6. 로그인 상태로 전환

**출력 항목**:
- 회원가입 성공 메시지
- 이메일 인증 안내 메시지
- 자동 로그인

**에러 처리**:
- 이메일 중복: "이미 사용 중인 이메일입니다"
- 약관 미동의: "약관에 동의해주세요"
- 비밀번호 불일치: "비밀번호가 일치하지 않습니다"

**권한**: 공개 (모든 사용자)

---

### 2.3 비밀번호 재설정

**기능 설명**: 비밀번호를 잊은 경우 재설정

**입력 항목**:
- 이메일 주소 (필수)

**처리 과정**:
1. 이메일 주소 입력
2. Supabase Auth의 `resetPasswordForEmail()` 호출
3. Supabase가 자동으로 재설정 링크 생성 및 이메일 발송
4. 링크 클릭 시 비밀번호 변경 페이지로 이동
5. 새 비밀번호 입력 및 확인
6. Supabase Auth의 `updateUser()` 호출하여 비밀번호 변경

**출력 항목**:
- 이메일 발송 완료 메시지
- 비밀번호 변경 완료 메시지

**에러 처리**:
- 존재하지 않는 이메일: "등록되지 않은 이메일입니다"
- 링크 만료: "링크가 만료되었습니다"

**권한**: 공개 (모든 사용자)

---

### 2.4 사용자 프로필 관리

#### 2.4.1 프로필 조회
**기능 설명**: 현재 로그인한 사용자의 프로필 정보 조회

**데이터 소스**: 
- Supabase Auth (이메일, 프로필 이미지)
- PostgreSQL `users` 테이블 (추가 정보)

**출력 항목**:
- 닉네임
- 이메일
- 프로필 이미지
- 가입일
- 마지막 로그인 시간

**권한**: 로그인 사용자 (본인만)

---

#### 2.4.2 프로필 수정
**기능 설명**: 사용자 프로필 정보 수정

**입력 항목**:
- 닉네임 (선택, 최대 20자)
- 프로필 이미지 (선택, 이미지 파일)

**처리 과정**:
1. 수정할 정보 입력
2. 프로필 이미지 업로드 (선택)
   - Supabase Storage의 `avatars/{userId}` 버킷에 업로드
   - 업로드된 이미지 URL 획득
3. PostgreSQL `users` 테이블 업데이트
4. 변경사항 즉시 반영

**출력 항목**:
- 수정 완료 메시지
- 업데이트된 프로필 정보

**에러 처리**:
- 이미지 업로드 실패: "이미지 업로드에 실패했습니다"
- 네트워크 오류: "네트워크 오류가 발생했습니다"

**권한**: 로그인 사용자 (본인만)

---

### 2.5 로그아웃

**기능 설명**: 현재 세션 종료

**처리 과정**:
1. Supabase Auth의 `signOut()` 호출
2. 로컬 스토리지 데이터 삭제
3. 로그인 페이지로 리다이렉트

**출력 항목**:
- 로그아웃 완료 메시지

**권한**: 로그인 사용자

---

## 3. 책 검색 및 등록

### 3.1 책 검색 기능

#### 3.1.1 ISBN 검색
**기능 설명**: ISBN 번호로 책 정보 검색

**기술 구현**:
- Next.js API Route: `/api/books/search`
- 외부 책 정보 API 호출 (네이버 도서 API 또는 알라딘 API)

**입력 항목**:
- ISBN 번호 (필수, 10자리 또는 13자리)

**처리 과정**:
1. ISBN 번호 입력
2. Next.js API Route 호출
3. 외부 책 정보 API 호출
4. 검색 결과 파싱
5. 책 정보 표시

**출력 항목**:
- 책 제목
- 저자
- 출판사
- 출판일
- 책 표지 이미지
- ISBN
- 책 소개

**에러 처리**:
- 잘못된 ISBN: "올바른 ISBN 번호를 입력해주세요"
- 검색 결과 없음: "검색 결과가 없습니다"
- API 오류: "검색 중 오류가 발생했습니다"

**권한**: 공개 (모든 사용자)

---

#### 3.1.2 제목/저자 검색
**기능 설명**: 키워드로 책 검색

**기술 구현**:
- Next.js API Route: `/api/books/search`
- Query 파라미터: `q` (검색어), `type` (제목/저자)

**입력 항목**:
- 검색 키워드 (필수, 최소 1자)

**처리 과정**:
1. 검색 키워드 입력
2. Next.js API Route 호출
3. 외부 책 정보 API 호출
4. 검색 결과 목록 표시
5. 페이지네이션 처리

**출력 항목**:
- 검색 결과 목록 (최대 100개)
- 각 결과: 제목, 저자, 출판사, 표지 이미지

**정렬 옵션**:
- 정확도순 (기본)
- 출판일순
- 판매량순

**권한**: 공개 (모든 사용자)

---

### 3.2 책 등록 기능

#### 3.2.1 검색 결과에서 등록
**기능 설명**: 검색 결과에서 선택한 책을 내 책 목록에 등록

**기술 구현**:
- Next.js API Route: `/api/books` (POST)
- PostgreSQL `books` 테이블에 책 정보 저장 (중복 체크)
- PostgreSQL `user_books` 테이블에 사용자-책 관계 저장

**입력 항목**:
- 독서 상태 (필수): `want_to_read`, `reading`, `completed`
- 읽기 시작일 (선택)
- 읽기 완료일 (선택)
- 별점 (선택): 1-5점
- 읽는 이유 (선택)
- 상세 소개 (선택)

**처리 과정**:
1. 검색 결과에서 책 선택
2. 책 정보 자동 입력
3. `books` 테이블에 책 정보 저장 (ISBN이 없으면 생성, 있으면 기존 책 사용)
4. `user_books` 테이블에 사용자-책 관계 저장
5. 추가 정보 입력 (선택)
6. 등록 완료 메시지 표시

**출력 항목**:
- 등록 완료 메시지
- 내 책 목록에 추가됨

**에러 처리**:
- 중복 등록: "이미 등록된 책입니다"
- 저장 실패: "책 등록에 실패했습니다"

**권한**: 로그인 사용자

---

#### 3.2.2 수동 책 등록
**기능 설명**: 검색 없이 직접 책 정보 입력하여 등록

**입력 항목**:
- 책 제목 (필수)
- 저자 (필수)
- 출판사 (선택)
- ISBN (선택)
- 책 표지 이미지 (선택)
- 독서 상태 (필수)
- 읽기 시작일 (선택)
- 읽기 완료일 (선택)
- 별점 (선택)
- 읽는 이유 (선택)
- 상세 소개 (선택)

**처리 과정**:
1. 책 정보 입력 폼 표시
2. 필수 정보 입력
3. 선택 정보 입력
4. 책 표지 이미지 업로드 (선택)
   - Supabase Storage의 `book-covers` 버킷에 업로드
5. PostgreSQL에 저장
   - `books` 테이블에 책 정보 저장
   - `user_books` 테이블에 사용자-책 관계 저장

**출력 항목**:
- 등록 완료 메시지

**권한**: 로그인 사용자

---

### 3.3 책 정보 수정

**기능 설명**: 등록된 책의 정보 수정

**기술 구현**:
- Next.js API Route: `/api/books/[id]` (PUT)
- PostgreSQL `user_books` 테이블 업데이트

**입력 항목**:
- 수정 가능한 모든 필드 (독서 상태, 시작일, 완료일, 별점, 읽는 이유, 상세 소개)

**처리 과정**:
1. 책 정보 수정 모달 열기
2. 수정할 정보 입력
3. PostgreSQL 업데이트
4. 변경사항 즉시 반영

**출력 항목**:
- 수정 완료 메시지

**권한**: 로그인 사용자 (본인이 등록한 책만)

---

### 3.4 책 삭제

**기능 설명**: 등록된 책 삭제

**기술 구현**:
- Next.js API Route: `/api/books/[id]` (DELETE)
- PostgreSQL `user_books` 테이블에서 삭제
- 관련 노트는 유지 (선택적으로 삭제 가능)

**처리 과정**:
1. 삭제 확인 다이얼로그 표시
2. 확인 시 PostgreSQL에서 삭제
3. 관련 노트는 유지 (사용자 선택에 따라 삭제 가능)

**출력 항목**:
- 삭제 완료 메시지

**권한**: 로그인 사용자 (본인이 등록한 책만)

---

## 4. 필사/사진/메모 관리 (노트 관리)

### 4.1 노트 등록

#### 4.1.1 필사 직접 입력
**기능 설명**: 인상 깊은 문장을 직접 입력하여 필사

**기술 구현**:
- Next.js API Route: `/api/notes` (POST)
- PostgreSQL `notes` 테이블에 저장
- 타입: `quote`

**입력 항목**:
- 필사 내용 (필수)
- 책 선택 (필수, 등록된 책 중 선택)
- 페이지 번호 (선택)
- 태그 (선택, 여러 개 가능)
- 공개 여부 (선택, 기본값: false)

**처리 과정**:
1. 필사 입력 화면 표시
2. 책 선택
3. 필사 내용 입력
4. 페이지 번호 입력 (선택)
5. 태그 추가 (선택)
6. PostgreSQL에 저장
7. 등록 완료

**출력 항목**:
- 등록 완료 메시지
- 노트 목록에 추가됨

**권한**: 로그인 사용자

---

#### 4.1.2 이미지 업로드 및 OCR
**기능 설명**: 책 페이지 이미지를 업로드하고 OCR로 텍스트 추출

**기술 구현**:
- Next.js API Route: `/api/notes/ocr` (POST)
- Supabase Storage에 이미지 업로드
- OCR API 호출 (Google Cloud Vision API 또는 Tesseract)
- PostgreSQL `notes` 테이블에 저장
- 타입: `quote` 또는 `photo`

**입력 항목**:
- 이미지 파일 (필수, jpg, png, 최대 10MB)
- 책 선택 (필수, 등록된 책 중 선택)
- 페이지 번호 (선택)

**처리 과정**:
1. 이미지 파일 선택
2. Supabase Storage의 `notes/{userId}/{noteId}` 버킷에 업로드
3. Next.js API Route에서 OCR API 호출
4. Google Cloud Vision API 또는 Tesseract로 텍스트 추출
5. 추출된 텍스트 표시 및 편집 가능
6. 사용자가 텍스트 확인 및 수정
7. PostgreSQL에 저장
   - 이미지 URL 저장
   - 추출된 텍스트를 `content` 필드에 저장

**출력 항목**:
- 업로드된 이미지 미리보기
- 추출된 텍스트 (편집 가능)

**에러 처리**:
- 파일 크기 초과: "파일 크기는 10MB 이하여야 합니다"
- 지원하지 않는 형식: "jpg, png 파일만 업로드 가능합니다"
- OCR 실패: "텍스트 추출에 실패했습니다. 다시 시도해주세요"

**권한**: 로그인 사용자

---

#### 4.1.3 책 페이지 사진 업로드
**기능 설명**: 책 페이지를 사진으로 촬영하여 저장

**기술 구현**:
- Next.js API Route: `/api/notes` (POST)
- Supabase Storage에 이미지 업로드
- PostgreSQL `notes` 테이블에 저장
- 타입: `photo`

**입력 항목**:
- 이미지 파일 (필수, jpg, png, 최대 10MB)
- 책 선택 (필수)
- 페이지 번호 (선택)

**처리 과정**:
1. 카메라로 책 페이지 촬영 또는 갤러리에서 선택
2. Supabase Storage에 업로드
3. PostgreSQL에 저장
4. 해당 책의 노트로 자동 연결

**출력 항목**:
- 업로드 완료 메시지
- 노트 목록에 추가됨

**권한**: 로그인 사용자

---

#### 4.1.4 메모 작성
**기능 설명**: 책에 대한 생각이나 감상을 메모로 남기기

**기술 구현**:
- Next.js API Route: `/api/notes` (POST)
- PostgreSQL `notes` 테이블에 저장
- 타입: `memo`

**입력 항목**:
- 메모 내용 (필수)
- 책 선택 (필수)
- 페이지 번호 (선택)
- 태그 (선택)

**처리 과정**:
1. 메모 입력 화면 표시
2. 책 선택
3. 메모 내용 입력
4. 페이지 번호 입력 (선택)
5. 태그 추가 (선택)
6. PostgreSQL에 저장

**출력 항목**:
- 등록 완료 메시지
- 노트 목록에 추가됨

**권한**: 로그인 사용자

---

### 4.2 노트 조회

#### 4.2.1 내 노트 목록 조회
**기능 설명**: 사용자가 등록한 노트 목록 조회

**기술 구현**:
- Next.js API Route: `/api/notes` (GET)
- PostgreSQL `notes` 테이블 조회 (userId로 필터링)
- Full-text search 인덱스 활용

**데이터 소스**: 
- PostgreSQL `notes` 테이블 (userId로 필터링)

**출력 항목**:
- 노트 목록 (카드 또는 리스트 형태)
- 각 노트: 타입 아이콘, 내용 미리보기, 책 제목, 날짜, 썸네일 이미지

**정렬 옵션**:
- 최신순 (기본)
- 오래된순
- 책 제목순
- 페이지 순서순

**필터링**:
- 전체
- 타입별 (필사/사진/메모)
- 책별 필터링
- 날짜별 필터링
- 태그별 필터링

**권한**: 로그인 사용자 (본인 노트만)

---

#### 4.2.2 책별 노트 조회
**기능 설명**: 특정 책에 대한 노트 목록 조회

**기술 구현**:
- Next.js API Route: `/api/books/[id]/notes` (GET)
- PostgreSQL `notes` 테이블 조회 (bookId로 필터링)

**입력 항목**:
- 책 ID (URL 파라미터)

**출력 항목**:
- 해당 책의 노트 목록
- 책 정보 (제목, 저자, 표지)
- 페이지 순서대로 정렬

**권한**: 로그인 사용자 (본인이 등록한 책의 노트만)

---

#### 4.2.3 노트 상세 조회
**기능 설명**: 개별 노트의 상세 정보 조회

**기술 구현**:
- Next.js API Route: `/api/notes/[id]` (GET)
- PostgreSQL `notes` 테이블 조회

**입력 항목**:
- 노트 ID (URL 파라미터 또는 모달)

**출력 항목**:
- 노트 타입 (필사/사진/메모)
- 노트 내용
- 노트 이미지 (있는 경우)
- 책 정보 (제목, 저자, 표지)
- 페이지 번호
- 태그
- 생성일/수정일
- 공유 버튼

**권한**: 로그인 사용자 (본인 노트만, 공개 노트는 모든 사용자)

---

### 4.3 노트 수정

**기능 설명**: 등록된 노트 정보 수정

**기술 구현**:
- Next.js API Route: `/api/notes/[id]` (PUT)
- PostgreSQL `notes` 테이블 업데이트

**입력 항목**:
- 수정 가능한 모든 필드 (내용, 페이지, 태그, 공개 여부)
- 이미지 교체 (선택)

**처리 과정**:
1. 노트 수정 모달 열기
2. 수정할 정보 입력
3. 이미지 교체 시 새 이미지 업로드
4. PostgreSQL 업데이트
5. 변경사항 즉시 반영

**출력 항목**:
- 수정 완료 메시지

**권한**: 로그인 사용자 (본인 노트만)

---

### 4.4 노트 삭제

**기능 설명**: 등록된 노트 삭제

**기술 구현**:
- Next.js API Route: `/api/notes/[id]` (DELETE)
- PostgreSQL `notes` 테이블에서 삭제
- Supabase Storage에서 이미지 삭제 (있는 경우)

**처리 과정**:
1. 삭제 확인 다이얼로그 표시
2. 확인 시 PostgreSQL에서 삭제
3. Supabase Storage에서 이미지 삭제
4. 삭제 완료

**출력 항목**:
- 삭제 완료 메시지

**권한**: 로그인 사용자 (본인 노트만)

---

### 4.5 노트 공유

#### 4.5.1 카드뉴스 형태로 공유
**기능 설명**: 선택한 노트를 카드뉴스 형태로 자동 변환하여 SNS에 공유

**기술 구현**:
- Next.js API Route: `/api/notes/[id]/share` (POST)
- Canvas API 또는 서버 사이드 이미지 생성
- 다양한 템플릿 제공

**입력 항목**:
- 노트 ID
- 템플릿 선택 (선택)

**처리 과정**:
1. 노트 선택
2. 공유 버튼 클릭
3. 템플릿 선택 (선택)
4. 카드뉴스 이미지 생성
5. SNS 공유 옵션 표시

**출력 항목**:
- 생성된 카드뉴스 이미지
- 인스타그램 공유 버튼
- 카카오톡 공유 버튼
- 이미지 다운로드 버튼

**지원 플랫폼**:
- 인스타그램
- 카카오톡
- 블로그 (이미지 다운로드)

**권한**: 로그인 사용자 (본인 노트만)

---

#### 4.5.2 공유 링크 생성
**기능 설명**: 노트를 공유할 수 있는 링크 생성

**기술 구현**:
- Next.js API Route: `/api/notes/[id]/share-link` (POST)
- PostgreSQL `shared_links` 테이블에 공유 정보 저장
- 공유 링크 URL 생성: `/shared/[linkId]`

**처리 과정**:
1. 노트 상세 페이지에서 "공유하기" 버튼 클릭
2. PostgreSQL에 공유 정보 저장
3. 공유 링크 URL 생성
4. 링크 복사 기능 제공

**출력 항목**:
- 공유 링크 URL
- 링크 복사 버튼
- 만료 시간 안내 (30일)

**데이터 구조**:
```sql
CREATE TABLE shared_links (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  note_id UUID REFERENCES notes(id) ON DELETE CASCADE,
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  expires_at TIMESTAMPTZ DEFAULT (NOW() + INTERVAL '30 days'),
  UNIQUE(note_id, user_id)
);
```

**권한**: 로그인 사용자 (본인 노트만)

---

#### 4.5.3 공유 링크 조회
**기능 설명**: 공유 링크를 통해 노트 조회 (로그인 불필요)

**기술 구현**:
- Next.js 페이지: `/shared/[linkId]`
- PostgreSQL `shared_links` 테이블 조회

**입력 항목**:
- 링크 ID (URL 파라미터)

**처리 과정**:
1. 링크 ID로 `shared_links` 테이블 조회
2. 만료 시간 확인
3. 만료되지 않았으면 노트 정보 표시
4. 만료되었으면 만료 메시지 표시

**출력 항목**:
- 책 표지 이미지
- 책 제목, 저자
- 노트 이미지 (있는 경우)
- 노트 내용
- 노트 날짜, 페이지

**에러 처리**:
- 링크 없음: "존재하지 않는 링크입니다"
- 링크 만료: "만료된 링크입니다"

**권한**: 공개 (모든 사용자)

---

## 5. 기록 자동 정리

### 5.1 기록 자동 분류

**기능 설명**: 작성한 필사, 사진, 메모가 해당 책으로 자동 분류

**기술 구현**:
- 노트 작성 시 현재 선택된 책으로 자동 연결
- PostgreSQL Foreign Key 제약조건 활용

**처리 과정**:
1. 사용자가 책을 선택한 상태에서 노트 작성
2. 노트가 자동으로 해당 책의 `book_id`로 연결
3. `notes` 테이블에 저장 시 자동 분류 완료

**수용 기준**:
- 자동 분류 정확도 95% 이상
- 분류 오류 시 사용자가 수동으로 수정 가능
- 자동 정리 완료 시간 10초 이내

**권한**: 로그인 사용자

---

### 5.2 페이지 순서 자동 정렬

**기능 설명**: 기록이 페이지 순서대로 자동 정렬

**기술 구현**:
- PostgreSQL 쿼리에서 `ORDER BY page_number, created_at` 사용

**처리 과정**:
1. 노트 조회 시 페이지 번호 기준으로 정렬
2. 페이지 번호가 없는 노트는 작성 시간순으로 정렬
3. 사용자가 정렬 방식을 변경할 수 있음

**출력 항목**:
- 페이지 순서대로 정렬된 노트 목록

**권한**: 로그인 사용자

---

### 5.3 기존 사진 불러오기 및 자동 분류

**기능 설명**: 갤러리의 기존 사진을 불러와서 자동으로 책에 분류

**기술 구현**:
- Next.js API Route: `/api/notes/bulk-import` (POST)
- 이미지 분석 (선택적, Gemini API 활용)
- 사용자가 수동으로 책 선택

**입력 항목**:
- 여러 이미지 파일 (선택)
- 책 선택 (필수)

**처리 과정**:
1. 갤러리에서 여러 사진 선택
2. 책 선택
3. 선택한 사진들을 일괄 업로드
4. 각 사진을 노트로 저장
5. 해당 책으로 자동 분류

**출력 항목**:
- 업로드 완료 메시지
- 생성된 노트 목록

**권한**: 로그인 사용자

---

## 6. 검색 기능

### 6.1 문장 단위 전체 텍스트 검색

**기능 설명**: 저장한 문장의 일부를 입력하여 검색

**기술 구현**:
- Next.js API Route: `/api/notes/search` (GET)
- PostgreSQL Full-Text Search 활용
- `to_tsvector`와 `to_tsquery` 사용

**입력 항목**:
- 검색 키워드 (필수)

**처리 과정**:
1. 검색어 입력
2. PostgreSQL Full-Text Search 쿼리 실행
3. 검색 결과 표시
4. 관련도 순으로 정렬

**출력 항목**:
- 검색 결과 목록
- 각 결과: 노트 내용, 책 제목, 날짜, 페이지 번호

**수용 기준**:
- 검색 응답 시간 1초 이내
- 검색 정확도 90% 이상
- 검색 결과 관련도 순 정렬

**권한**: 로그인 사용자 (본인 노트만)

---

### 6.2 책 제목으로 검색

**기능 설명**: 책 제목으로 노트 검색

**기술 구현**:
- PostgreSQL JOIN 쿼리 활용
- `books` 테이블과 `notes` 테이블 조인

**입력 항목**:
- 책 제목 (필수)

**처리 과정**:
1. 책 제목 입력
2. `books` 테이블에서 책 검색
3. 해당 책의 모든 노트 조회

**출력 항목**:
- 해당 책의 모든 노트 목록

**권한**: 로그인 사용자

---

### 6.3 날짜로 검색

**기능 설명**: 날짜 범위로 노트 검색

**기술 구현**:
- PostgreSQL `WHERE created_at BETWEEN` 쿼리

**입력 항목**:
- 시작일 (필수)
- 종료일 (필수)

**처리 과정**:
1. 날짜 범위 선택
2. PostgreSQL 쿼리 실행
3. 해당 기간의 노트 목록 표시

**출력 항목**:
- 날짜 범위 내의 모든 노트 목록
- 날짜별로 그룹화하여 표시 가능

**권한**: 로그인 사용자

---

### 6.4 태그/주제로 검색

**기능 설명**: 태그나 주제로 노트 검색

**기술 구현**:
- PostgreSQL 배열 검색 (`tags @> ARRAY['태그명']`)

**입력 항목**:
- 태그 (선택, 여러 개 가능)

**처리 과정**:
1. 태그 선택
2. PostgreSQL 배열 검색 쿼리 실행
3. 해당 태그가 있는 노트 목록 표시

**출력 항목**:
- 태그별 노트 목록

**권한**: 로그인 사용자

---

### 6.5 필사 유형으로 검색

**기능 설명**: 필사, 사진, 메모 유형으로 필터링

**기술 구현**:
- PostgreSQL `WHERE type = 'quote'` 쿼리

**입력 항목**:
- 노트 타입 (필사/사진/메모)

**처리 과정**:
1. 노트 타입 선택
2. PostgreSQL 쿼리 실행
3. 해당 타입의 노트 목록 표시

**출력 항목**:
- 타입별 노트 목록

**권한**: 로그인 사용자

---

### 6.6 AI 기반 의미 검색 (Phase 2)

**기능 설명**: 자연어로 질문하여 의미 기반 검색

**기술 구현**:
- Next.js API Route: `/api/notes/ai-search` (POST)
- Google Gemini API 활용
- 의미적 유사도 기반 검색

**입력 항목**:
- 자연어 질의 (예: "인생에 대한 깨달음이 담긴 문장")

**처리 과정**:
1. 자연어 질의 입력
2. Gemini API로 의미 분석
3. 노트 내용과 의미적 유사도 계산
4. 유사도 순으로 정렬하여 표시

**출력 항목**:
- 의미적으로 유사한 노트 목록
- 유사도 점수 표시

**수용 기준**:
- 검색 응답 시간 3초 이내
- 의미적 관련도 높은 결과 제공

**권한**: 로그인 사용자

---

## 7. 독서 타임라인

### 7.1 시간순 독서 기록 조회

**기능 설명**: 시간순으로 정리된 독서 기록을 한눈에 확인

**기술 구현**:
- Next.js API Route: `/api/timeline` (GET)
- PostgreSQL 쿼리: `ORDER BY created_at DESC`
- 무한 스크롤 구현

**입력 항목**:
- 시작 날짜 (선택)
- 종료 날짜 (선택)

**처리 과정**:
1. 타임라인 페이지 접속
2. PostgreSQL에서 시간순으로 노트 조회
3. 날짜별로 그룹화하여 표시
4. 무한 스크롤로 이전 기록 로드

**출력 항목**:
- 시간순으로 정렬된 노트 목록
- 날짜별 그룹화
- 책 표지, 노트 미리보기

**수용 기준**:
- 타임라인 로딩 시간 3초 이내
- 모바일 반응형 지원
- 스크롤 최적화

**권한**: 로그인 사용자 (본인 기록만)

---

### 7.2 읽은 책 목록 조회

**기능 설명**: 읽은 책 목록을 확인

**기술 구현**:
- Next.js API Route: `/api/books` (GET)
- PostgreSQL `user_books` 테이블 조회
- `status = 'completed'` 필터링

**출력 항목**:
- 읽은 책 목록
- 각 책: 표지, 제목, 저자, 완료일, 노트 수

**권한**: 로그인 사용자

---

### 7.3 타임라인 PDF/이미지 내보내기

**기능 설명**: 타임라인을 PDF나 이미지로 내보내기

**기술 구현**:
- Next.js API Route: `/api/timeline/export` (POST)
- PDF 생성 라이브러리 (예: jsPDF, Puppeteer)
- 이미지 생성 (Canvas API)

**입력 항목**:
- 내보내기 형식 (PDF/이미지)
- 기간 선택 (선택)

**처리 과정**:
1. 내보내기 형식 선택
2. 기간 선택 (선택)
3. 타임라인 데이터 조회
4. PDF 또는 이미지 생성
5. 다운로드 제공

**출력 항목**:
- 생성된 PDF 또는 이미지 파일
- 다운로드 링크

**수용 기준**:
- 내보내기 시간 10초 이내
- 고해상도 이미지 제공

**권한**: 로그인 사용자

---

## 8. 독서모임 기능

### 8.1 독서모임 생성

**기능 설명**: 새로운 독서모임 생성

**기술 구현**:
- Next.js API Route: `/api/groups` (POST)
- PostgreSQL `reading_groups` 테이블에 저장
- 생성자를 모임 리더로 설정

**입력 항목**:
- 모임 이름 (필수, 최대 50자)
- 모임 설명 (선택, 최대 500자)
- 모임 이미지 (선택)

**처리 과정**:
1. 모임 정보 입력
2. PostgreSQL에 저장
3. 생성자를 모임 리더로 설정
4. 생성자를 모임 멤버로 추가 (`group_members` 테이블)
5. 모임 생성 완료

**출력 항목**:
- 모임 생성 완료 메시지
- 모임 상세 페이지로 이동

**수용 기준**:
- 모임 생성 시간 1분 이내

**권한**: 로그인 사용자

---

### 8.2 독서모임 목록 조회

**기능 설명**: 참여 중인 독서모임 목록 조회

**기술 구현**:
- Next.js API Route: `/api/groups` (GET)
- PostgreSQL `reading_groups`와 `group_members` 조인

**데이터 소스**: 
- PostgreSQL `reading_groups` 테이블
- `group_members` 테이블에서 현재 사용자 ID로 필터링

**출력 항목**:
- 모임 목록
- 각 모임: 모임명, 설명, 멤버 수, 생성일

**권한**: 로그인 사용자

---

### 8.3 독서모임 상세 정보

**기능 설명**: 모임의 상세 정보 조회

**기술 구현**:
- Next.js API Route: `/api/groups/[id]` (GET)
- PostgreSQL 조인 쿼리

**입력 항목**:
- 모임 ID (URL 파라미터)

**출력 항목**:
- 모임 기본 정보 (이름, 설명, 이미지)
- 멤버 목록
- 모임 활동 내역
- 읽는 책 리스트

**권한**: 로그인 사용자 (모임 멤버만)

---

### 8.4 모임 멤버 관리

#### 8.4.1 멤버 초대
**기능 설명**: 모임에 새 멤버 초대

**기술 구현**:
- Next.js API Route: `/api/groups/[id]/members` (POST)
- 초대 링크 생성 또는 이메일 초대

**입력 항목**:
- 초대할 사용자 이메일 또는 사용자 ID
- 또는 초대 링크 생성

**처리 과정**:
1. 초대할 사용자 정보 입력 또는 초대 링크 생성
2. 사용자 존재 확인
3. `group_members` 테이블에 추가
4. 초대 완료

**출력 항목**:
- 초대 완료 메시지
- 초대 링크 (생성한 경우)

**권한**: 모임 리더

---

#### 8.4.2 멤버 제거
**기능 설명**: 모임에서 멤버 제거

**기술 구현**:
- Next.js API Route: `/api/groups/[id]/members/[userId]` (DELETE)

**처리 과정**:
1. 제거할 멤버 선택
2. 확인 다이얼로그 표시
3. 확인 시 `group_members` 테이블에서 제거

**출력 항목**:
- 제거 완료 메시지

**권한**: 모임 리더

---

### 8.5 모임에서 읽는 책 관리

#### 8.5.1 책 추가
**기능 설명**: 모임에서 읽을 책 추가

**기술 구현**:
- Next.js API Route: `/api/groups/[id]/books` (POST)
- PostgreSQL `group_books` 테이블에 저장

**입력 항목**:
- 책 ID (필수)
- 읽기 기간 (선택)

**처리 과정**:
1. 책 검색 및 선택
2. 읽기 기간 설정 (선택)
3. `group_books` 테이블에 저장
4. 모임 구성원들에게 표시

**출력 항목**:
- 책 추가 완료 메시지
- 모임 책 리스트에 추가됨

**권한**: 모임 리더

---

#### 8.5.2 책 목록 조회
**기능 설명**: 모임에서 읽는 책 목록 조회

**기술 구현**:
- PostgreSQL `group_books`와 `books` 조인

**출력 항목**:
- 모임 책 목록
- 각 책: 표지, 제목, 저자, 읽기 기간

**권한**: 모임 멤버

---

### 8.6 구성원 진행 현황 확인

**기능 설명**: 구성원들의 읽기 진행 상황을 확인

**기술 구현**:
- Next.js API Route: `/api/groups/[id]/progress` (GET)
- PostgreSQL `group_activities` 테이블 조회
- Supabase Realtime으로 실시간 업데이트

**출력 항목**:
- 구성원별 읽기 진행률
- 구성원별 노트 수
- 최근 활동 내역

**수용 기준**:
- 실시간 진행 현황 업데이트 (Supabase Realtime 활용)

**권한**: 모임 멤버

---

### 8.7 구성원 노트 공유

**기능 설명**: 구성원의 노트를 모임에 공유

**기술 구현**:
- 노트 작성 시 "모임에 공유" 옵션 선택
- PostgreSQL `group_activities` 테이블에 저장
- Supabase Realtime으로 실시간 피드 업데이트

**입력 항목**:
- 노트 ID
- 모임 ID

**처리 과정**:
1. 노트 작성 시 "모임에 공유" 옵션 선택
2. `group_activities` 테이블에 저장
3. 모임 피드에 표시

**출력 항목**:
- 모임 피드에 공유된 노트 표시
- 구성원들이 댓글을 남길 수 있음 (선택적)

**권한**: 모임 멤버

---

### 8.8 모임 활동 요약

**기능 설명**: 모임 활동 요약 제공

**기술 구현**:
- Next.js API Route: `/api/groups/[id]/summary` (GET)
- PostgreSQL 집계 쿼리

**출력 항목**:
- 주간/월간 활동 통계
- 구성원별 활동 통계
- 가장 많이 공유된 노트
- 모임 활동 그래프

**권한**: 모임 멤버

---

### 8.9 모임 관리자 기능

#### 8.9.1 모임 설정 변경
**기능 설명**: 모임 정보 수정

**기술 구현**:
- Next.js API Route: `/api/groups/[id]` (PUT)
- PostgreSQL `reading_groups` 테이블 업데이트

**입력 항목**:
- 모임명
- 모임 설명
- 모임 이미지

**권한**: 모임 리더

---

#### 8.9.2 모임 삭제
**기능 설명**: 모임 삭제

**기술 구현**:
- Next.js API Route: `/api/groups/[id]` (DELETE)
- PostgreSQL CASCADE 삭제

**처리 과정**:
1. 삭제 확인 다이얼로그 표시
2. 확인 시 모임 삭제
3. 관련 데이터 정리 (CASCADE)

**권한**: 모임 리더

---

## 9. 관리자 기능

### 9.1 관리자 대시보드

**기능 설명**: 시스템 전체 통계 및 상태 조회

**기술 구현**:
- Next.js API Route: `/api/admin/dashboard` (GET)
- PostgreSQL 집계 쿼리

**출력 항목**:
- 전체 사용자 수
- 전체 책 등록 수
- 전체 노트 수
- 일일/월일 활동 통계
- 시스템 상태

**권한**: 관리자만

---

### 9.2 사용자 관리

**기능 설명**: 사용자 정보 조회 및 관리

**기능**:
- 사용자 목록 조회
- 사용자 상세 정보 조회
- 사용자 권한 변경
- 사용자 계정 정지/해제

**기술 구현**:
- Next.js API Route: `/api/admin/users` (GET, PUT, DELETE)
- PostgreSQL `users` 테이블 관리

**권한**: 관리자만

---

### 9.3 데이터 관리

**기능 설명**: 시스템 데이터 관리

**기능**:
- 샘플 책 관리
- 샘플 노트 관리
- 데이터 마이그레이션
- 데이터 백업/복원

**기술 구현**:
- Next.js API Route: `/api/admin/data` (POST, PUT, DELETE)
- PostgreSQL 백업/복원 스크립트

**권한**: 관리자만

---

## 10. UI/UX 기능

### 10.1 반응형 디자인

**기능 설명**: 다양한 화면 크기에 대응

**지원 화면**:
- 모바일 (320px 이상)
- 태블릿 (768px 이상)
- 데스크톱 (1024px 이상)
- 와이드 스크린 (1920px 이상)

**특징**:
- 모바일: 카드 형태 레이아웃
- 데스크톱: 테이블 형태 레이아웃
- 자동 레이아웃 전환
- Tailwind CSS 반응형 클래스 활용

---

### 10.2 모바일 네비게이션

**기능 설명**: 모바일 환경을 위한 네비게이션

**기능**:
- 하단 네비게이션 바
- 햄버거 메뉴
- 터치 제스처 지원

---

### 10.3 로딩 및 피드백

**기능 설명**: 사용자 피드백 제공

**기능**:
- 로딩 스피너
- 진행률 표시
- 성공 메시지
- 에러 메시지
- 토스트 알림

---

### 10.4 모달 및 다이얼로그

**기능 설명**: 다양한 모달 및 다이얼로그

**종류**:
- 책 정보 편집 모달
- 노트 등록/수정 모달
- 노트 상세 보기 모달
- 확인 다이얼로그
- 에러 다이얼로그

---

## 11. 보안 및 권한

### 11.1 데이터 접근 제어

**규칙**:
- 사용자는 본인의 데이터만 접근 가능
- 공개 노트는 모든 사용자가 조회 가능
- 모임 멤버는 모임 데이터 접근 가능
- 관리자는 모든 데이터 접근 가능

**구현**:
- Supabase Row Level Security (RLS)
- PostgreSQL RLS 정책
- 클라이언트 측 권한 검증

---

### 11.2 파일 업로드 보안

**제한사항**:
- 파일 타입: jpg, png만 허용
- 파일 크기: 최대 10MB
- 이미지 검증

**처리**:
- 파일 타입 검증
- 파일 크기 검증
- 이미지 최적화
- Supabase Storage 보안 규칙

---

### 11.3 인증 보안

**기능**:
- 토큰 기반 인증 (Supabase JWT)
- 세션 관리
- 자동 로그아웃 (일정 시간 미사용 시)
- 비밀번호 암호화 (Supabase 자동 처리)

---

## 12. 성능 최적화

### 12.1 데이터 로딩

**기능**:
- 지연 로딩 (Lazy Loading)
- 페이지네이션
- 데이터 캐싱 (React Query 또는 SWR)
- 프리페칭

---

### 12.2 이미지 최적화

**기능**:
- 이미지 리사이징
- 이미지 압축
- 지연 로딩
- WebP 형식 지원
- Next.js Image 컴포넌트 활용

---

### 12.3 코드 최적화

**기능**:
- 코드 스플리팅
- 번들 최적화
- 트리 쉐이킹
- 불필요한 리렌더링 방지
- Next.js 최적화 기능 활용

---

## 13. 에러 처리

### 13.1 일반 에러 처리

**에러 유형**:
- 네트워크 오류
- 인증 오류
- 권한 오류
- 데이터 검증 오류
- 서버 오류

**처리 방법**:
- 사용자 친화적 에러 메시지
- 에러 로깅 (Vercel Logs)
- 자동 재시도 (일부 경우)
- 에러 리포트

---

### 13.2 특정 에러 시나리오

#### 13.2.1 로그인 실패
- "이메일 또는 비밀번호가 올바르지 않습니다"
- "계정이 비활성화되었습니다"
- "네트워크 오류가 발생했습니다"

#### 13.2.2 데이터 저장 실패
- "데이터 저장에 실패했습니다. 다시 시도해주세요"
- "권한이 없습니다"
- "네트워크 오류가 발생했습니다"

#### 13.2.3 파일 업로드 실패
- "파일 업로드에 실패했습니다"
- "파일 크기가 너무 큽니다"
- "지원하지 않는 파일 형식입니다"

---

## 14. 데이터 구조

### 14.1 PostgreSQL 데이터베이스 스키마

#### users 테이블
```sql
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email TEXT UNIQUE NOT NULL,
  name TEXT,
  avatar_url TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

#### books 테이블
```sql
CREATE TABLE books (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  isbn TEXT UNIQUE,
  title TEXT NOT NULL,
  author TEXT,
  publisher TEXT,
  cover_image_url TEXT,
  description TEXT,
  published_date DATE,
  category TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_books_title ON books(title);
CREATE INDEX idx_books_author ON books(author);
CREATE INDEX idx_books_isbn ON books(isbn);
```

#### user_books 테이블 (독서 기록)
```sql
CREATE TABLE user_books (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  book_id UUID REFERENCES books(id) ON DELETE CASCADE,
  status TEXT CHECK (status IN ('want_to_read', 'reading', 'completed')) DEFAULT 'want_to_read',
  started_at DATE,
  completed_at DATE,
  rating INTEGER CHECK (rating >= 1 AND rating <= 5),
  reading_reason TEXT,
  review TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(user_id, book_id)
);

CREATE INDEX idx_user_books_user_id ON user_books(user_id);
CREATE INDEX idx_user_books_book_id ON user_books(book_id);
CREATE INDEX idx_user_books_status ON user_books(status);
```

#### notes 테이블 (필사/메모/사진)
```sql
CREATE TABLE notes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  book_id UUID REFERENCES books(id) ON DELETE CASCADE,
  type TEXT CHECK (type IN ('quote', 'photo', 'memo')) NOT NULL,
  content TEXT,
  image_url TEXT,
  page_number INTEGER,
  tags TEXT[] DEFAULT '{}',
  is_public BOOLEAN DEFAULT false,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Full-text search를 위한 인덱스
CREATE INDEX idx_notes_content_search ON notes USING gin(to_tsvector('korean', content));
CREATE INDEX idx_notes_user_book ON notes(user_id, book_id);
CREATE INDEX idx_notes_type ON notes(type);
CREATE INDEX idx_notes_created_at ON notes(created_at DESC);
CREATE INDEX idx_notes_tags ON notes USING gin(tags);
```

#### reading_groups 테이블 (독서모임)
```sql
CREATE TABLE reading_groups (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  description TEXT,
  leader_id UUID REFERENCES users(id) ON DELETE CASCADE,
  image_url TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_reading_groups_leader_id ON reading_groups(leader_id);
```

#### group_members 테이블 (모임 멤버)
```sql
CREATE TABLE group_members (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  group_id UUID REFERENCES reading_groups(id) ON DELETE CASCADE,
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  joined_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(group_id, user_id)
);

CREATE INDEX idx_group_members_group_id ON group_members(group_id);
CREATE INDEX idx_group_members_user_id ON group_members(user_id);
```

#### group_books 테이블 (모임에서 읽는 책)
```sql
CREATE TABLE group_books (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  group_id UUID REFERENCES reading_groups(id) ON DELETE CASCADE,
  book_id UUID REFERENCES books(id) ON DELETE CASCADE,
  start_date DATE,
  end_date DATE,
  added_at TIMESTAMPTZ DEFAULT NOW(),
  added_by UUID REFERENCES users(id),
  UNIQUE(group_id, book_id)
);

CREATE INDEX idx_group_books_group_id ON group_books(group_id);
CREATE INDEX idx_group_books_book_id ON group_books(book_id);
```

#### group_activities 테이블 (모임 활동)
```sql
CREATE TABLE group_activities (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  group_id UUID REFERENCES reading_groups(id) ON DELETE CASCADE,
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  book_id UUID REFERENCES books(id) ON DELETE CASCADE,
  note_id UUID REFERENCES notes(id) ON DELETE SET NULL,
  activity_type TEXT CHECK (activity_type IN ('note_shared', 'progress_updated', 'book_added')) DEFAULT 'note_shared',
  progress INTEGER, -- 읽은 페이지/퍼센트
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_group_activities_group_id ON group_activities(group_id);
CREATE INDEX idx_group_activities_user_id ON group_activities(user_id);
CREATE INDEX idx_group_activities_created_at ON group_activities(created_at DESC);
```

#### shared_links 테이블 (공유 링크)
```sql
CREATE TABLE shared_links (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  note_id UUID REFERENCES notes(id) ON DELETE CASCADE,
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  expires_at TIMESTAMPTZ DEFAULT (NOW() + INTERVAL '30 days'),
  UNIQUE(note_id, user_id)
);

CREATE INDEX idx_shared_links_note_id ON shared_links(note_id);
CREATE INDEX idx_shared_links_expires_at ON shared_links(expires_at);
```

---

### 14.2 Row Level Security (RLS) 정책

#### notes 테이블 RLS
```sql
-- RLS 활성화
ALTER TABLE notes ENABLE ROW LEVEL SECURITY;

-- 사용자는 자신의 노트만 조회/수정 가능
CREATE POLICY "Users can view own notes" ON notes
  FOR SELECT USING (auth.uid() = user_id);

CREATE POLICY "Users can insert own notes" ON notes
  FOR INSERT WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can update own notes" ON notes
  FOR UPDATE USING (auth.uid() = user_id);

CREATE POLICY "Users can delete own notes" ON notes
  FOR DELETE USING (auth.uid() = user_id);

-- 공개된 노트는 모든 사용자가 조회 가능
CREATE POLICY "Public notes are viewable by everyone" ON notes
  FOR SELECT USING (is_public = true);
```

#### user_books 테이블 RLS
```sql
ALTER TABLE user_books ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own user_books" ON user_books
  FOR SELECT USING (auth.uid() = user_id);

CREATE POLICY "Users can insert own user_books" ON user_books
  FOR INSERT WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can update own user_books" ON user_books
  FOR UPDATE USING (auth.uid() = user_id);

CREATE POLICY "Users can delete own user_books" ON user_books
  FOR DELETE USING (auth.uid() = user_id);
```

---

### 14.3 Supabase Storage 구조

```
Supabase Storage
├── avatars/
│   └── {userId}/
│       └── avatar.{ext}
├── book-covers/
│   └── {bookId}/
│       └── cover.{ext}
└── notes/
    └── {userId}/
        └── {noteId}/
            └── image.{ext}
```

---

## 15. API 명세

### 15.1 Next.js API Routes

#### 인증 관련
- **`/api/auth/login`** (POST): 이메일/비밀번호 로그인
- **`/api/auth/signup`** (POST): 회원가입
- **`/api/auth/logout`** (POST): 로그아웃
- **`/api/auth/reset-password`** (POST): 비밀번호 재설정

#### 책 관련
- **`/api/books/search`** (GET): 책 검색 (ISBN, 제목, 저자)
- **`/api/books`** (GET, POST): 책 목록 조회, 책 등록
- **`/api/books/[id]`** (GET, PUT, DELETE): 책 상세 조회, 수정, 삭제
- **`/api/books/[id]/notes`** (GET): 책별 노트 조회

#### 노트 관련
- **`/api/notes`** (GET, POST): 노트 목록 조회, 노트 등록
- **`/api/notes/[id]`** (GET, PUT, DELETE): 노트 상세 조회, 수정, 삭제
- **`/api/notes/search`** (GET): 노트 검색 (Full-text search)
- **`/api/notes/ocr`** (POST): 이미지 OCR 처리
- **`/api/notes/[id]/share`** (POST): 카드뉴스 생성
- **`/api/notes/[id]/share-link`** (POST): 공유 링크 생성
- **`/api/notes/bulk-import`** (POST): 일괄 이미지 업로드
- **`/api/notes/ai-search`** (POST): AI 기반 의미 검색 (Phase 2)

#### 타임라인 관련
- **`/api/timeline`** (GET): 독서 타임라인 조회
- **`/api/timeline/export`** (POST): 타임라인 내보내기

#### 독서모임 관련
- **`/api/groups`** (GET, POST): 모임 목록 조회, 모임 생성
- **`/api/groups/[id]`** (GET, PUT, DELETE): 모임 상세 조회, 수정, 삭제
- **`/api/groups/[id]/members`** (GET, POST, DELETE): 멤버 관리
- **`/api/groups/[id]/books`** (GET, POST): 모임 책 관리
- **`/api/groups/[id]/progress`** (GET): 진행 현황 조회
- **`/api/groups/[id]/summary`** (GET): 모임 활동 요약

#### 관리자 관련
- **`/api/admin/dashboard`** (GET): 관리자 대시보드
- **`/api/admin/users`** (GET, PUT, DELETE): 사용자 관리
- **`/api/admin/data`** (POST, PUT, DELETE): 데이터 관리

---

### 15.2 외부 API

#### 책 정보 API
- **Naver Books API**: `https://openapi.naver.com/v1/search/book.json`
- **알라딘 API**: 책 정보 검색

#### OCR API
- **Google Cloud Vision API**: 이미지 텍스트 추출
- 또는 **Tesseract**: 오픈소스 OCR

#### AI/LLM API
- **Google Gemini API**: 의미 기반 검색, 유사 문장 찾기

---

## 16. 배포 및 인프라

### 16.1 Vercel 배포

**배포 프로세스**:
1. GitHub/GitLab 저장소에 코드 푸시
2. Vercel이 자동으로 빌드 및 배포
3. 프리뷰 배포 (Pull Request별)
4. 프로덕션 배포 (메인 브랜치)

**환경 변수 설정**:
- `NEXT_PUBLIC_SUPABASE_URL`: Supabase 프로젝트 URL
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`: Supabase Anon Key
- `SUPABASE_SERVICE_ROLE_KEY`: Supabase Service Role Key
- `GEMINI_API_KEY`: Google Gemini API 키
- `NAVER_CLIENT_ID`: 네이버 API 클라이언트 ID
- `NAVER_CLIENT_SECRET`: 네이버 API 클라이언트 시크릿

---

### 16.2 Supabase 설정

**프로젝트 설정**:
- PostgreSQL 데이터베이스 생성
- Storage 버킷 생성 (avatars, book-covers, notes)
- Authentication Provider 설정 (카카오, 구글)
- RLS 정책 설정

---

## 17. 테스트 및 디버깅

### 17.1 테스트 기능

**기능**:
- Supabase 연결 테스트
- 인증 테스트
- 데이터 접근 테스트
- 권한 테스트

**접근 경로**:
- `/test/supabase`: Supabase 연결 테스트
- `/test/auth`: 인증 테스트
- `/test/rls`: RLS 정책 테스트

**권한**: 개발 환경에서만 접근 가능

---

### 17.2 디버깅 도구

**기능**:
- 로그인 상태 확인
- 데이터 구조 확인
- 권한 확인
- 네트워크 요청 모니터링
- Vercel Logs 활용

---

## 18. 향후 계획 기능

### 18.1 독서 리포트 (Phase 2)
- 읽은 책 수 통계
- 독서 패턴 분석
- 장르별 분포
- 월별/연도별 통계
- Gemini API를 활용한 인사이트 생성

### 18.2 AI 기반 추천 (Phase 2)
- 책 추천
- 노트 추천
- 독서 패턴 기반 추천
- Gemini API 활용

### 18.3 크리에이터 커뮤니티 (Phase 2)
- 구독자와 함께 읽기 활동
- 커뮤니티 형성
- 팬덤 관리

### 18.4 알림 기능 (Phase 2)
- 독서 알림
- 모임 활동 알림
- 시스템 알림
- Supabase Realtime 활용

---

## 19. 부록

### 19.1 용어 정의

| 용어 | 설명 |
|------|------|
| 노트 | 필사(quote), 사진(photo), 메모(memo)를 통칭하는 용어 |
| 필사 | 책의 인상적인 구절을 텍스트로 저장하는 기능 |
| RLS | Row Level Security, 행 수준 보안 |
| OCR | Optical Character Recognition, 광학 문자 인식 |
| OAuth | Open Authorization, 개방형 인증 프로토콜 |
| Full-Text Search | 전체 텍스트 검색, PostgreSQL의 to_tsvector 활용 |

---

### 19.2 참고 문서
- PRD- Readtree 독서플랫폼.md: 제품 요구사항 문서
- user_stories.md: 사용자 스토리 명세서
- 구버전-기능명세서.md: Firebase 기반 구버전 명세서

---

### 19.3 변경 이력

| 버전 | 날짜 | 변경 내용 | 작성자 |
|------|------|----------|--------|
| 3.0.0 | 2025-12 | Supabase + Vercel 기반 신버전 기능명세서 작성 | 개발팀 |

---

**문서 끝**

